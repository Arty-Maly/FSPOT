<!-- search -->
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
    	<div class="row">
			<div class="col-xs-8 ">
				<%= form_tag '/main', :method => 'get' do %>
				<%= text_field_tag :search, nil, class: "form-control" %>
			</div>
			
				<%= submit_tag "search", :name => nil, :class => "btn btn-default" %>	

			<% end %>
		</div>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<!-- slider -->
<div class="container">
  <div class="lazy">
	<% @uploads.each do |upload| %>
	<div>
		<img id="slider" data-lazy =<%= image_url upload.picture_url %>>
	</div>
    <% end %>
  </div>
</div>

<!--upload-->

<div class="upload">
  <a href="/upload/index" type="button" class="btn btn-default btn-sm">Upload</a>
</div>



